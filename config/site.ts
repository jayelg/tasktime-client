export type SiteConfig = typeof siteConfig

export const siteConfig = {
  name: 'tasktime',
  description: 'A tool to capture the state of a project.',
  links: {
    github: 'https://github.com/jayelg/tasktime-client'
  }
}
